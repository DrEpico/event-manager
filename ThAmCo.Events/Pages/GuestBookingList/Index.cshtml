@page
@model ThAmCo.Events.Pages.GuestBookingList.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GuestBooking[0].HasAttended)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GuestBooking[0].Guest)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GuestBooking[0].Event)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.GuestBooking) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HasAttended)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Guest.GuestId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.EventId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.GuestBookingId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.GuestBookingId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.GuestBookingId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
